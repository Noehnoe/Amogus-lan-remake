// render.js — Canvas drawing helpers (Phase 3+)
// Phase 1 stub — nothing to render yet.

const canvas  = document.getElementById('gameCanvas');
const ctx     = canvas.getContext('2d');

// Will be populated in Phase 3 with the render loop and draw functions.
